package shapes;

class Square extends Rectangle {
   protected double side;

   public Square(double length, double width) {
//       super(length, width);
       this.length = length;
       this.width = width;

   }

   pub


}
